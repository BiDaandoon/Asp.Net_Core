
## **پیاده‌سازی لایه دامنه میکروسرویس کاربر:**

لایه **دامنه** قلب میکروسرویس کاربر است که منطق اصلی کسب‌وکار، قوانین و مدل‌هایی را که نحوه عملکرد عملیات مرتبط با کاربر را تعریف می‌کنند، در بر می‌گیرد. این لایه مستقل از چارچوب است، به این معنی که مستقل از هرگونه نگرانی فنی مانند پایگاه‌های داده یا APIها عمل می‌کند و تضمین می‌کند که مدل کسب‌وکار قابل استفاده مجدد، قابل آزمایش و سازگار باقی بماند.

در این لایه، ما **موجودیت‌هایی** (مثلاً User، Address، RefreshToken، Client) را تعریف می‌کنیم که نمایانگر اشیاء تجاری دنیای واقعی هستند، و همچنین **رابط‌های مخزن** (IUserRepository) را تعریف می‌کنیم که نحوه دسترسی و دستکاری این موجودیت‌ها را تعریف می‌کنند.

این تفکیک دغدغه‌ها تضمین می‌کند:

- قوانین کسب‌وکار متمرکز هستند و با پیاده‌سازی فنی آمیخته نشده‌اند.
- این مدل می‌تواند بدون بازنویسی منطق اصلی، با تغییرات فناوری آینده سازگار شود.
- یک ساختار معماری تمیز که در آن « **چه**» از « **چگونه**» جدا شده است .

مسئولیت‌های کلیدی عبارتند از:

- نمایش داده‌ها و روابط کاربر.
- تعریف قراردادها برای عملیات پایداری.
- مدیریت قوانین دامنه مانند اعتبار توکن، فعال‌سازی حساب کاربری و مدیریت آدرس.

##### **نصب بسته‌های مورد نیاز**

در پروژه دامنه، لطفاً بسته زیر را نصب کنید تا بتوانیم از ویژگی‌های مرتبط با EF Core مستقیماً روی موجودیت‌ها استفاده کنیم.

- **نصب بسته Microsoft.EntityFrameworkCore**

ابتدا، دو پوشه در دایرکتوری ریشه پروژه با نام‌های **Entities** و **Repositories ایجاد کنید.**

##### **موجودیت‌ها** / **User.cs**

یک فایل کلاس به نام **User.cs** در **پوشه Entities** خود **پروژه UserService.Domain** ایجاد کنید و کد زیر را کپی و جایگذاری کنید. موجودیت User نمایانگر پروفایل اصلی کاربر در مدل دامنه است و تمام اطلاعات ضروری کاربر، از جمله شناسه منحصر به فرد، نام کاربری، ایمیل، شماره تلفن، نام کامل، URL عکس پروفایل، وضعیت تأیید و فعال‌سازی ایمیل، مهرهای زمانی برای ایجاد و آخرین ورود و مجموعه‌ای از آدرس‌های مرتبط را در خود جایگذاری می‌کند.

```csharp
using System;
using System.Collections.Generic;
using Microsoft.EntityFrameworkCore;

namespace UserService.Domain.Entities
{
    [Index(nameof(Email), Name = "Index_Email_Unique", IsUnique = true)]
    public class User
    {
        public Guid Id { get; set; }
        public string? UserName { get; set; } = null!;
        public string? Email { get; set; } = null!;
        public bool IsEmailConfirmed { get; set; }
        public bool IsActive { get; set; }
        public string? PhoneNumber { get; set; }
        public string? FullName { get; set; }
        public string? ProfilePhotoUrl { get; set; }
        public DateTime CreatedAt { get; set; }
        public DateTime? LastLoginAt { get; set; }
        public List<Address> Addresses { get; set; } = new();
    }
}
```

##### **Entities/Address.cs**

یک فایل کلاس به نام **Address.cs** در **پوشه Entities** خود **پروژه UserService.Domain** ایجاد کنید و کد زیر را کپی و جایگذاری کنید. موجودیت Address اطلاعات آدرس فیزیکی مرتبط با یک کاربر را مدل‌سازی می‌کند، به همراه ویژگی‌هایی برای شناسه منحصر به فرد، شناسه کاربری مالک، خطوط آدرس، شهر، استان، کد پستی، کشور و پرچم‌های بولی برای نشان دادن آدرس‌های پیش‌فرض حمل و نقل و صورتحساب. این ساختار به هر کاربر اجازه می‌دهد تا چندین آدرس را مدیریت کند و از الزامات حمل و نقل/صورتحساب تجارت الکترونیک پشتیبانی کند.

```csharp
using System;

namespace UserService.Domain.Entities
{
    public class Address
    {
        public Guid Id { get; set; }
        public Guid UserId { get; set; }
        public string AddressLine1 { get; set; } = null!;
        public string? AddressLine2 { get; set; }
        public string City { get; set; } = null!;
        public string State { get; set; } = null!;
        public string PostalCode { get; set; } = null!;
        public string Country { get; set; } = null!;
        public bool IsDefaultShipping { get; set; }
        public bool IsDefaultBilling { get; set; }
    }
}
```

##### **موجودیت‌ها** / **Client.cs**

یک فایل کلاس به نام **Client.cs** در **پوشه Entities** خود **پروژه UserService.Domain** ایجاد کنید و کد زیر را کپی و جایگذاری کنید. این موجودیت اطلاعات کلاینت را در خود نگه می‌دارد.

```csharp
using System.Collections.Generic;
using Microsoft.EntityFrameworkCore;

namespace UserService.Domain.Entities
{
    [Index(nameof(ClientName), Name = "Index_ClientName_Unique", IsUnique = true)]
    public class Client
    {
        public string ClientId { get; set; } = null!;
        public string ClientName { get; set; } = null!; // e.g., "Web", "Android", "iOS"
        public string? Description { get; set; }
        public bool IsActive { get; set; }
        public ICollection<RefreshToken> RefreshTokens { get; set; } = new List<RefreshToken>();
    }
}
```

##### **Entities/RefreshToken.cs**

یک فایل کلاس به نام **RefreshToken.cs** در **پوشه Entities** خود **پروژه UserService.Domain** ایجاد کنید و کد زیر را کپی و جایگذاری کنید. موجودیت RefreshToken نشان دهنده یک توکن امنیتی است که برای تمدید توکن‌های دسترسی JWT بدون نیاز به ورود مجدد کاربر استفاده می‌شود و شامل شناسه منحصر به فرد، UserId مرتبط، رشته توکن، مهرهای زمانی برای ایجاد، انقضا و لغو اختیاری و همچنین ردیابی IP برای رویدادهای ایجاد و ابطال است. این شامل منطقی برای تعیین فعال یا منقضی بودن توکن است که امنیت جلسه را تقویت می‌کند.

```csharp
using System;

namespace UserService.Domain.Entities
{
    public class RefreshToken
    {
        public Guid Id { get; set; }
        public Guid UserId { get; set; }
        public string Token { get; set; } = null!;
        public string ClientId { get; set; } = null!;    // Web/Android/iOS etc.
        public Client Client { get; set; } = null!;
        public string UserAgent { get; set; } = null!;   // Chrome, Firefox, Safari, etc.
        public DateTime CreatedAt { get; set; }
        public string CreatedByIp { get; set; } = null!;
        public DateTime? RevokedAt { get; set; }
        public string? RevokedByIp { get; set; }
        public DateTime ExpiresAt { get; set; }
        public bool IsActive => RevokedAt == null && !IsExpired;
        public bool IsExpired => DateTime.UtcNow >= ExpiresAt;
    }
}
```

##### **مخازن/IUserRepository.cs**

رابط IUserRepository قرارداد مربوط به تمام عملیات دسترسی و دستکاری داده‌های مرتبط با کاربر در دامنه، از جمله یافتن کاربران از طریق ایمیل، نام کاربری یا شناسه، ایجاد کاربران، اعتبارسنجی رمزهای عبور، به‌روزرسانی داده‌های کاربر، مدیریت نقش‌های کاربر، مدیریت توکن‌های ایمیل و رمز عبور، ردیابی ورود به سیستم، بررسی‌های قفل و دو مرحله‌ای، چرخه عمر توکن تازه‌سازی و مدیریت آدرس را تعریف می‌کند. با انتزاع این عملیات، منطق دامنه را از پایداری داده‌ها جدا می‌کند و از قابلیت آزمایش و توسعه‌پذیری پشتیبانی می‌کند.

یک فایل کلاس با نام **IUserRepository.cs** در **پوشه Repositories** خود **پروژه UserService.Domain** ایجاد کنید و کد زیر را کپی و جایگذاری کنید:

```csharp
using System;
using System.Collections.Generic;
using System.Threading.Tasks;
using UserService.Domain.Entities;

namespace UserService.Domain.Repositories
{
    public interface IUserRepository
    {
        Task<User?> FindByEmailAsync(string email);
        Task<User?> FindByUserNameAsync(string userName);
        Task<User?> FindByIdAsync(Guid id);
        Task<bool> CreateUserAsync(User user, string password);
        Task<bool> CheckPasswordAsync(User user, string password);
        Task<bool> UpdateUserAsync(User user);
        Task<IList<string>> GetUserRolesAsync(User user);
        Task<bool> AddUserToRoleAsync(User user, string role);
        Task<bool> VerifyConfirmaionEmailAsync(User user, string token);
        Task<string?> GenerateEmailConfirmationTokenAsync(User user);
        Task<string?> GeneratePasswordResetTokenAsync(User user);
        Task<bool> ResetPasswordAsync(User user, string token, string newPassword);
        Task<bool> ChangePasswordAsync(User user, string currentPassword, string newPassword);
        Task UpdateLastLoginAsync(User user, DateTime loginTime);
        Task<string> GenerateAndStoreRefreshTokenAsync(Guid userId, string clientId, string userAgent, string ipAddress);
        Task<RefreshToken?> GetRefreshTokenAsync(string token);
        Task RevokeRefreshTokenAsync(RefreshToken refreshToken, string ipAddress);
        Task<List<Address>> GetAddressesByUserIdAsync(Guid userId);
        Task<Guid> AddOrUpdateAddressAsync(Address address);
        Task<bool> DeleteAddressAsync(Guid userId, Guid addressId);
        Task<bool> IsLockedOutAsync(User user);
        Task<bool> IsTwoFactorEnabledAsync(User user);
        Task IncrementAccessFailedCountAsync(User user);
        Task ResetAccessFailedCountAsync(User user);
        Task<DateTime?> GetLockoutEndDateAsync(User user);
        Task<int> GetMaxFailedAccessAttemptsAsync();
        Task<int> GetAccessFailedCountAsync(User user);
        Task<bool> IsValidClientAsync(string clientId);
        Task<bool> IsUserExistsAsync(Guid userId);
        Task<Address?> GetAddressByUserIdAndAddressIdAsync(Guid userId, Guid addressId);
    }
}
```

لایه دامنه به عنوان **پایه و اساس** میکروسرویس کاربر عمل می‌کند و طرح کلی تمام عملیات مرتبط با کاربر را ارائه می‌دهد. با عاری نگه داشتن این لایه از وابستگی‌های فنی، سیستم قابلیت نگهداری طولانی مدت، مقیاس‌پذیری و انعطاف‌پذیری را برای نیازهای در حال تحول کسب و کار به دست می‌آورد.