## **نماینده‌های عمومی در سی شارپ با مثال‌های بلادرنگ**

در این مقاله، قصد دارم **به همراه مثال‌هایی، در مورد Generic Delegateها در سی‌شارپ** به همراه مثال‌ها پرداختیم، مطالعه کنید بحث کنم. لطفاً مقاله قبلی ما را که در آن به بررسی مثال‌های بلادرنگ Delegate در سی‌شارپ . به عنوان بخشی از این مقاله، قصد داریم نکات زیر را به تفصیل مورد بحث قرار دهیم.

1. **Generic Delegate ها در سی شارپ چیستند؟**
2. **انواع نمایندگان عمومی در سی شارپ**
3. **چرا به نمایندگان عمومی نیاز داریم؟**
4. **تابع Generic Delegate در سی شارپ؟**
5. **نماینده عمومی اکشن در C#؟**
6. **نماینده عمومی پیش فرض در C#؟**
7. **مثال‌هایی برای درک Generic Delegateها در سی‌شارپ**

##### **Generic Delegate ها در سی شارپ چیستند؟**

Generic Delegates در سی شارپ به عنوان بخشی از .NET Framework 3.5 معرفی شدند **که** نیازی به تعریف نمونه delegate برای فراخوانی متدها ندارد. برای درک Generic Delegates در سی شارپ، باید دانش اولیه در مورد **Delegates** داشته باشید .

##### **انواع Generic Delegate ها در سی شارپ**

سی شارپ سه نماینده عمومی داخلی ارائه می‌دهد که به شرح زیر هستند:

1. **عملکرد**
2. **اکشن**
3. **گزاره**

##### **چرا در سی شارپ به Generic Delegate ها نیاز داریم؟**

بیایید با یک مثال، نیاز به Generic Delegateها در سی‌شارپ را درک کنیم. برای درک این موضوع، ابتدا نحوه استفاده از Delegateها برای فراخوانی متدها را بررسی کنیم. فرض کنید سه متد زیر را داریم و می‌خواهیم این متدها را با استفاده از Delegateها فراخوانی کنیم.

![چرا در سی شارپ به Generic Delegate ها نیاز داریم؟](https://dotnettutorials.net/wp-content/uploads/2022/09/word-image-29911-1-9.png "چرا در سی شارپ به Generic Delegate ها نیاز داریم؟")

همانطور که در کد بالا مشاهده می‌کنید، **متد AddNumber1** سه پارامتر می‌گیرد و مقداری از نوع double را برمی‌گرداند. به طور مشابه، **متد AddNumber2** سه پارامتر می‌گیرد اما هیچ مقداری را برنمی‌گرداند و در اینجا نوع داده‌ی برگشتی void است. متد سوم، یعنی **متد CheckLength،** یک پارامتر رشته‌ای می‌گیرد و یک مقدار بولی برمی‌گرداند. اگر طول رشته بیشتر از ۵ باشد، مقدار true و در غیر این صورت مقدار false را برمی‌گرداند.

حال اگر بخواهیم سه متد فوق را با استفاده از delegate ها در سی شارپ فراخوانی کنیم، باید سه delegate ایجاد کنیم که امضای آنها باید با امضای سه متد فوق مطابق تصویر زیر مطابقت داشته باشد.

![نماینده‌های عمومی در سی شارپ با مثال‌های بلادرنگ](https://dotnettutorials.net/wp-content/uploads/2022/09/word-image-29911-2-7.png "نماینده‌های عمومی در سی شارپ با مثال‌های بلادرنگ")

همانطور که در تصویر بالا مشاهده می‌کنید، ما سه نماینده (delegates) ایجاد می‌کنیم. اکنون، پس از ایجاد نمایندگان، می‌توانیم با ایجاد نمونه‌هایی از هر نماینده که به روش‌های مربوطه ارجاع می‌دهند، متدها را فراخوانی کنیم و سپس می‌توانیم نماینده را همانطور که در کد زیر نشان داده شده است، فراخوانی کنیم.

![نماینده‌های عمومی در سی شارپ با مثال‌های بلادرنگ](https://dotnettutorials.net/wp-content/uploads/2022/09/word-image-29911-3-9.png "نماینده‌های عمومی در سی شارپ با مثال‌های بلادرنگ")

##### **مثالی برای فراخوانی متدها با استفاده از Custom Delegate در سی شارپ:**

هر آنچه که تا الان مورد بحث قرار داده‌ایم، کد کامل در زیر آمده است.

```csharp
using System;

namespace GenericDelegatesDemo
{
    public class GenericDelegates
    {
        public delegate double AddNumber1Delegate(int no1, float no2, double no3);
        public delegate void AddNumber2Delegate(int no1, float no2, double no3);
        public delegate bool CheckLengthDelegate(string name);

        static void Main(string[] args)
        {
            AddNumber1Delegate obj1 = new AddNumber1Delegate(AddNumber1);
            double Result = obj1.Invoke(100, 125.45f, 456.789);
            Console.WriteLine(Result);

            AddNumber2Delegate obj2 = new AddNumber2Delegate(AddNumber2);
            obj2.Invoke(50, 255.45f, 123.456);

            CheckLengthDelegate obj3 = new CheckLengthDelegate(CheckLength);
            bool Status = obj3.Invoke("Pranaya");
            Console.WriteLine(Status);

            Console.ReadKey();
        }

        public static double AddNumber1(int no1, float no2, double no3)
        {
            return no1 + no2 + no3;
        }

        public static void AddNumber2(int no1, float no2, double no3)
        {
            Console.WriteLine(no1 + no2 + no3);
        }

        public static bool CheckLength(string name)
        {
            if (name.Length > 5)
                return true;
            return false;
        }
    }
}
```

###### **خروجی:**

![مثالی برای فراخوانی متدها با استفاده از Custom Delegate در سی شارپ](https://dotnettutorials.net/wp-content/uploads/2022/09/word-image-29911-4-5.png "مثالی برای فراخوانی متدها با استفاده از Custom Delegate در سی شارپ")

در حال حاضر، این روش استفاده از Delegateها برای فراخوانی متدها است. سوالی که به ذهنمان می‌رسد این است که آیا واقعاً برای فراخوانی متدها در C# نیاز به ایجاد Delegateهای سفارشی داریم؟

##### **آیا واقعاً برای فراخوانی متدها در سی شارپ به ایجاد Delegate های سفارشی نیاز داریم؟**

پاسخ خیر است. چارچوب C#.NET تعدادی نماینده عمومی (generic delegate) ارائه می‌دهد که می‌توانند این کار را برای ما انجام دهند. C# سه نماینده عمومی (generic Delegates) ارائه می‌دهد. آنها به شرح زیر هستند.

1. **عملکرد**
2. **اکشن**
3. **گزاره**

حالا، بیایید ادامه دهیم و سعی کنیم هر سه delegate عمومی فوق را درک کنیم. بیایید سعی کنیم بفهمیم که همه این delegate ها چه هستند، چه زمانی و چگونه می توان از همه این delegate های عمومی در C# با مثال استفاده کرد.

##### **تابع Generic Delegate در سی شارپ چیست؟**

نماینده عمومی Func در C# در فضای نام System وجود دارد. این نماینده یک یا چند پارامتر ورودی را دریافت کرده و یک پارامتر خروجی را برمی‌گرداند. آخرین پارامتر به عنوان مقدار بازگشتی در نظر گرفته می‌شود. نماینده عمومی Func در C# می‌تواند تا 16 پارامتر ورودی از انواع داده متفاوت یا یکسان را دریافت کند. باید یک نوع بازگشتی داشته باشد. نوع بازگشتی اجباری است اما پارامتر ورودی اجباری نیست.

**نکته:** هر زمان که delegate شما مقداری را برمی‌گرداند، چه با دریافت پارامتر ورودی و چه بدون آن، باید از delegate نوع Func Generic در سی شارپ استفاده کنید.

##### **Action Generic Delegate در سی شارپ چیست؟**

نماینده عمومی اکشن (Action Generic Delegate) در سی شارپ (C#) نیز در فضای نام System وجود دارد. این نماینده یک یا چند پارامتر ورودی می‌گیرد و چیزی برنمی‌گرداند. این نماینده می‌تواند حداکثر ۱۶ پارامتر ورودی از انواع داده متفاوت یا یکسان را بپذیرد.

**نکته:** هر زمان که delegate شما هیچ مقداری را برنمی‌گرداند، چه با گرفتن پارامتر ورودی و چه بدون آن، باید از delegate از نوع Action Generic در سی شارپ استفاده کنید.

##### **نماینده عمومی Predicate در C# چیست؟**

نماینده عمومی Predicate در C# نیز در فضای نام System وجود دارد. این نماینده برای تأیید معیارهای خاص متد استفاده می‌شود و خروجی را به صورت Boolean، True یا False، برمی‌گرداند. این نماینده یک پارامتر ورودی می‌گیرد و همیشه یک مقدار Boolean را برمی‌گرداند که اجباری است. این نماینده می‌تواند حداکثر ۱ پارامتر ورودی بگیرد و همیشه مقدار نوع Boolean را برمی‌گرداند.

**نکته:** هر زمان که delegate شما با دریافت تنها یک پارامتر ورودی، یک مقدار بولی (Boolean) برمی‌گرداند، باید از delegate از نوع Predicate Generic در سی شارپ استفاده کنید.

##### **مثال‌هایی برای درک Generic Delegateها در سی‌شارپ**

اجازه دهید سه delegate ژنریک بالا را در سی شارپ با یک مثال درک کنیم. در مثال اول، ما سه متد ایجاد کردیم،

1. متد **AddNumber1** سه پارامتر می‌گیرد و یک **مقدار double** استفاده خواهیم کرد . **برمی‌گرداند. در اینجا ما از Func Generic Delegate** برای رسیدن به همان چیزی که در مثال اول به آن دست یافتیم،
2. به طور مشابه، **متد AddNumber2** سه پارامتر می‌گیرد اما هیچ مقداری را برنمی‌گرداند. در اینجا ما از **Action Generic Delegate** برای دستیابی به همان چیزی که در مثال اول به آن دست یافتیم، استفاده خواهیم کرد.
3. متد **CheckLength** یک پارامتر ورودی رشته‌ای می‌گیرد و یک مقدار بولی برمی‌گرداند. در اینجا ما از **Predicate Generic Delegate** برای دستیابی به همان چیزی که در مثال اول به آن دست یافتیم، استفاده خواهیم کرد.

بیایید ابتدا سه نماینده‌ای را که در برنامه خود ایجاد کرده‌ایم حذف کنیم و ببینیم چگونه می‌توانیم همان برنامه را با استفاده از نمایندگان عمومی تبدیل کنیم.

##### **چگونه از Func Generic Delegate در سی شارپ استفاده کنیم؟**

نماینده عمومی Func در C# هر زمان که نماینده شما مقداری را برمی‌گرداند، چه با دریافت هر پارامتر ورودی و چه بدون آن، استفاده می‌شود. در مثال ما، متد AddNumber1 مقداری ورودی می‌گیرد و یک خروجی را برمی‌گرداند. بنابراین، امضای متد AddNumber1 با امضای نماینده عمومی Func مطابقت دارد. بنابراین، در اینجا، به جای ایجاد نماینده خودمان برای فراخوانی متد AddNumber1، می‌توانیم از نماینده عمومی Func برای فراخوانی متد AddNumber1 همانطور که در کد زیر نشان داده شده است، استفاده کنیم.

![چگونه از Func Generic Delegate در سی شارپ استفاده کنیم؟](https://dotnettutorials.net/wp-content/uploads/2022/09/word-image-29911-5-6.png "چگونه از Func Generic Delegate در سی شارپ استفاده کنیم؟")

همانطور که در کد بالا نشان داده شده است، Func Generic Delegate چهار پارامتر می‌گیرد، سه پارامتر اول پارامترهای ورودی و آخرین پارامتر مقدار بازگشتی است. به سازنده Func Generic Delegate، متد AddNumber1 را ارسال می‌کنیم که قرار است هنگام فراخوانی Func delegate اجرا شود.

##### **چگونه از Action Generic Delegate در سی شارپ استفاده کنیم؟**

نماینده عمومی Action در C# هر زمان که نماینده شما هیچ مقداری را برنمی‌گرداند، چه با گرفتن پارامتر ورودی و چه بدون آن، استفاده می‌شود. در مثال ما، متد AddNumber2 مقداری ورودی می‌گیرد اما هیچ مقداری را برنمی‌گرداند. بنابراین، امضای متد AddNumber2 با امضای نماینده عمومی Action مطابقت دارد. بنابراین، در اینجا، به جای ایجاد نماینده خودمان برای فراخوانی متد AddNumber2، می‌توانیم از نماینده عمومی Action برای فراخوانی متد AddNumber2 همانطور که در کد زیر نشان داده شده است، استفاده کنیم.

![چگونه از Action Generic Delegate در سی شارپ استفاده کنیم؟](https://dotnettutorials.net/wp-content/uploads/2022/09/word-image-29911-6-5.png "چگونه از Action Generic Delegate در سی شارپ استفاده کنیم؟")

همانطور که در کد بالا نشان داده شده است، Action Generic Delegate سه پارامتر ورودی می‌گیرد. به سازنده Action Generic Delegate، متد AddNumber2 را ارسال می‌کنیم که قرار است هنگام فراخوانی Action delegate اجرا شود. Action Generic Delegate هرگز هیچ مقداری را برنمی‌گرداند.

##### **چگونه از Predicate Generic Delegate در سی شارپ استفاده کنیم؟**

نماینده عمومی Predicate در C# هر زمان که نماینده شما با دریافت تنها یک پارامتر ورودی، یک مقدار Boolean برمی‌گرداند، استفاده می‌شود. در مثال ما، متد CheckLength یک پارامتر ورودی از نوع رشته می‌گیرد و یک مقدار Boolean برمی‌گرداند. بنابراین، امضای متد CheckLength با امضای نماینده عمومی Predicate مطابقت دارد. بنابراین، در اینجا، به جای ایجاد نماینده خودمان برای فراخوانی متد CheckLength، می‌توانیم از نماینده عمومی Predicate برای فراخوانی متد CheckLength همانطور که در کد زیر نشان داده شده است، استفاده کنیم.

![چگونه از Predicate Generic Delegate در سی شارپ استفاده کنیم؟](https://dotnettutorials.net/wp-content/uploads/2022/09/word-image-29911-7-3.png "چگونه از Predicate Generic Delegate در سی شارپ استفاده کنیم؟")

همانطور که در کد بالا نشان داده شده است، Predicate Generic Delegate یک پارامتر ورودی رشته‌ای می‌گیرد. به سازنده Predicate Generic Delegate، متد CheckLength را ارسال می‌کنیم که قرار است هنگام فراخوانی Predicate Generic delegate اجرا شود. این delegate می‌تواند حداکثر ۱ پارامتر ورودی بگیرد و ۰ مقدار برگرداند. به طور پیش‌فرض، یک مقدار Boolean را برمی‌گرداند.

##### **مثالی برای درک توابع (Func)، عمل (Action) و نماینده‌های ژنریک (Generic Delegates) در سی شارپ:**

هر آنچه که مورد بحث قرار دادیم، بیایید همه این موارد را در برنامه خود قرار دهیم. در مثال زیر، ما از سه نماینده عمومی به جای نماینده سفارشی خود برای فراخوانی سه متد استفاده می‌کنیم.

```csharp
using System;

namespace GenericDelegateDemo
{
    public class GenericDelegates
    {
        static void Main(string[] args)
        {
            Func<int, float, double, double> obj1 = new Func<int, float, double, double>(AddNumber1);
            double Result = obj1.Invoke(100, 125.45f, 456.789);
            Console.WriteLine(Result);

            Action<int, float, double> obj2 = new Action<int, float, double>(AddNumber2);
            obj2.Invoke(50, 255.45f, 123.456);

            Predicate<string> obj3 = new Predicate<string>(CheckLength);
            bool Status = obj3.Invoke("Pranaya");
            Console.WriteLine(Status);

            Console.ReadKey();
        }

        public static double AddNumber1(int no1, float no2, double no3)
        {
            return no1 + no2 + no3;
        }

        public static void AddNumber2(int no1, float no2, double no3)
        {
            Console.WriteLine(no1 + no2 + no3);
        }

        public static bool CheckLength(string name)
        {
            if (name.Length > 5)
                return true;
            return false;
        }
    }
}
```

###### **خروجی:**

![مثالی برای درک نماینده‌های ژنریک Func، Action و Predicate در سی شارپ](https://dotnettutorials.net/wp-content/uploads/2022/09/word-image-29911-8-3.png "مثالی برای درک نماینده‌های ژنریک Func، Action و Predicate در سی شارپ")

##### **نمایندگان عمومی با عبارات لامبدا در سی شارپ:**

بیایید ببینیم چگونه می‌توان از Lambda Expression به همراه Generic Delegates در سی‌شارپ استفاده کرد. مثال زیر همان مثال قبلی است. اما در این مورد، هنگام ایجاد نمونه‌ای از Generic Delegates، به جای متدها، از lambda expressions استفاده می‌کنیم و وقتی delegate را فراخوانی می‌کنیم، lambda expression مربوطه اجرا خواهد شد.

```csharp
using System;

namespace GenericDelegateDemo
{
    public class GenericDelegates
    {
        static void Main(string[] args)
        {
            Func<int, float, double, double> obj1 = (x, y, z) =>
            {
                return x + y + z;
            };

            double Result = obj1.Invoke(100, 125.45f, 456.789);
            Console.WriteLine(Result);

            Action<int, float, double> obj2 = (x, y, z) =>
            {
                Console.WriteLine(x + y + z);
            };
            obj2.Invoke(50, 255.45f, 123.456);

            Predicate<string> obj3 = new Predicate<string>(CheckLength);
            bool Status = obj3.Invoke("Pranaya");
            Console.WriteLine(Status);

            Console.ReadKey();
        }
        public static bool CheckLength(string name)
        {
            if (name.Length > 5)
                return true;
            return false;
        }
    }
}
```

###### **خروجی:**

![نمایندگان عمومی با عبارات لامبدا در سی شارپ](https://dotnettutorials.net/wp-content/uploads/2022/09/word-image-29911-9-3.png "نمایندگان عمومی با عبارات لامبدا در سی شارپ")

##### **نکاتی که باید هنگام کار با Generic Delegate های سی شارپ به خاطر داشته باشید:**

1. توابع (Func)، اکشن (Action) و پردیکات (Predicate) نماینده‌های (Delegate) داخلی و عمومی (Generic Inbuilt) هستند که در فضای نام System که در C# 3 معرفی شده است، وجود دارند.
2. هر سه این delegate ها را می توان با متد، **متد ناشناس** و **عبارات لامبدا** در سی شارپ استفاده کرد.
3. نماینده‌های Func می‌توانند حداکثر ۱۶ پارامتر ورودی داشته باشند و باید یک نوع بازگشتی داشته باشند و آن آخرین پارامتر در لیست پارامترها خواهد بود.
4. نماینده اکشن می‌تواند حداکثر ۱۶ پارامتر ورودی داشته باشد و هیچ نوع بازگشتی ندارد.
5. نماینده‌ی Predicate باید برخی از معیارهای یک متد را برآورده کند و فقط باید یک پارامتر ورودی داشته باشد. به طور پیش‌فرض، دارای یک پارامتر خروجی از نوع return است و لازم نیست پارامتر خروجی را به Predicate ارسال کنیم.